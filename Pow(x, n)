/* Implement pow(x, n), which calculates x raised to the power n (i.e., xn).

Constraints
-100.0 < x < 100.0
-231 <= n <= 231-1
n is an integer.
-104 <= xn <= 104 */

class Solution {
public:
    double myPow(double x, int n) {
        double ans;
        ans = pow(x, n);
        return ans;
    }
};
